SUBDIRS = unit
DIST_SUBDIRS = unit integration utils

# including framework/ as EXTRA_DIST, because we don't build it directly
# so including framework/ in DIST_SUBDIRS breaks source packaging.
EXTRA_DIST = framework
